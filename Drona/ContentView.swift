import SwiftUI

struct ContentView: View {

    var body: some View {
        VStack {
            Text("Drona Initialized")
                .font(.title)
                .padding()
        }
        .frame(minWidth: 500, minHeight: 300)
    }
}

#Preview {
    ContentView()
}
